import { primary, secondary, ternary, white} from '../../constants/style.constants';

export const styles = {
    summary: {
        "text-align": "center",
        display: "block",
        "margin-bottom": "20px",
    },
};

export default styles;
