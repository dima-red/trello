import { primary, secondary, ternary, white} from '../../constants/style.constants';

export const styles = {
    summary: {
        "text-align": "center",
        display: "block",
        "margin-bottom": "20px",
    },
    listNameButton: {
        padding: "5px 10px",
    },
};

export default styles;
